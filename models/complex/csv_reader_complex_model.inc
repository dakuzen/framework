<?
	/*

		$csv_reader_cmodel = CMODEL_CSV_READER::create($file);
		foreach($csv_reader_cmodel->iterator() as $row_index=>$row) {
			p($row);
		}

	*/

	class BASE_CMODEL_CSV_READER extends BASE_CMODEL_EXCEL_CSV {}